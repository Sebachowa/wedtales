<!DOCTYPE html>

<!-- This is for pages, not templates -->
<% if @livetemplate == true %>
  <%= render 'layouts/livetemplate' %>

  <!-- This is for templates -->
<% else %>

  <%= render 'layouts/main' %>

<% end %>
